import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '1.9_notas_aceso',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
