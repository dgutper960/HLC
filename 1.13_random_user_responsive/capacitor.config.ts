import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '1.13_random_user_responsive',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
